# Repository instructions

- Keep the exercise self-contained: production code must use only the Python standard library.
- Do not call real external systems. Tests and replay operate on in-memory ports and JSON fixtures.
- Keep `fixtures/historical_runs.json` unchanged.
- Run `python -m unittest discover -s tests -v` after changes.
